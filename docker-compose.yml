version: '3.9'

services:
  backendCRM:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./src:/app  # Sincroniza la carpeta src en el contenedor
    environment:
      - ENVIRONMENT=development
